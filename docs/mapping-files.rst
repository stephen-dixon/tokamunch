Mapping files
=============

Generate a blank mapping template from IDS schema paths:

.. code-block:: bash

   munchi init-mapping --ids magnetics

The generated JSON maps each template path to an empty object.

.. note::

   Command options are documented in :doc:`cli`.
