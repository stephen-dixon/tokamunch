Configuration
=============

Generate a starter configuration file with:

.. code-block:: bash

   munchi init-config

The full command-line reference is generated directly from the live parser in
:doc:`cli`.
