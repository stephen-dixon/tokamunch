CLI reference
=============

The reference below is generated directly from ``tokamunch.cli.build_parser()``,
so it stays aligned with the actual command-line interface.

.. argparse::
   :module: tokamunch.cli
   :func: build_parser
   :prog: munchi
